-- Create analytics schema
-- This schema holds views and tables ready for BI consumption

CREATE SCHEMA IF NOT EXISTS analytics;

COMMENT ON SCHEMA analytics IS 'Analytics layer with views and tables ready for BI tools';
