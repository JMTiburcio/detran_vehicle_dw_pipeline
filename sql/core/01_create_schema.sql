-- Create core schema
-- This schema holds the Data Warehouse layer with trusted, modeled data

CREATE SCHEMA IF NOT EXISTS core;

COMMENT ON SCHEMA core IS 'Data Warehouse layer with trusted, modeled and historical data';
